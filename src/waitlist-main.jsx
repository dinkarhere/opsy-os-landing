import React from 'react'
import ReactDOM from 'react-dom/client'
import Waitlist from './Waitlist.jsx'
import './reveal-fallback.js'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Waitlist />
  </React.StrictMode>,
)
